<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-9" />
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'nonce-2726c7f26c'">
    <meta name="viewport" content="width=device-width, initial-scale=0">
    <link rel="stylesheet" type="text/css" href="./css/dashboard.css">
    <script type="module" src="./js/dashboard.js"></script> 
  </head>
  <body>
    <div class="overview">
      <%- include('description'); %>
      <%- include('basic', { results }); %>
      <%- include ('piechart', { results }); %>
      <%- include('runAction'); %>
    </div>
    <div class="detail">
      <button class="accordion">BrowserObjectData Placeholder</button>
      <div class="panels">
	<%- include('panel', { results }); %>
      </div>
    </div>
    <div class="configuration">
      <h1 id="aectitle">Available Environment Configurations</h2>
      <%- include('browserOptions', { knownDevices }); %>
    </div>
  </body>
</html>
